//! Deprecated — the `MorselWorkerPool` in `pool.rs` is the only parallel
//! execution mechanism.  `ParallelPartitionCoordinator` has been removed.
//!
//! Shared utilities (`PartitionMessage`, `run_partition`, etc.) have moved
//! to `crate::executor::streaming::pool`.
